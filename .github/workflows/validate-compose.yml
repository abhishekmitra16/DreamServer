name: Validate Docker Compose

on:
  push:
    branches: [main]
    paths:
      - "dream-server/docker-compose*.yml"
      - "dream-server/compose/**"
  pull_request:
    branches: [main]
    paths:
      - "dream-server/docker-compose*.yml"
      - "dream-server/compose/**"

permissions:
  contents: read

jobs:
  validate-compose:
    name: Validate Docker Compose files
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      ## Commented out since there is no main docker compose file anymore
      # - name: Validate main docker-compose.yml
      #   run: |
      #     echo "Validating dream-server/docker-compose.yml"
      #     docker compose -f dream-server/docker-compose.yml config --quiet

      - name: Validate compose files in dream-server/
        run: |
          compose_files=$(find dream-server/ -name '*.yml' -type f 2>/dev/null || true)

          if [ -z "$compose_files" ]; then
            echo "No compose files found in dream-server/. Skipping validation."
            exit 0
          fi

          echo "Found compose files:"
          echo "$compose_files"
          echo ""

          failed=0
          for f in $compose_files; do
            echo "Validating $f ..."
            if docker compose -f "$f" config --quiet 2>&1; then
              echo "  OK"
            else
              echo "  FAILED"
              failed=1
            fi
          done

          if [ "$failed" -ne 0 ]; then
            echo ""
            echo "One or more compose files failed validation."
            exit 1
          fi

          echo ""
          echo "All compose files validated successfully."
